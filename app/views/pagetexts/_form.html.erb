<%= form_for(@pagetext) do |f| %>
  <% if @pagetext.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@pagetext.errors.count, "error") %> prohibited this pagetext from being saved:</h2>

      <ul>
      <% @pagetext.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :text %><br />
    <%= f.text_area :text %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
